# Where the FastAPI backend lives. Read only on the server, by the route handlers in
# src/app/api that forward the browser's /api/* calls, so it is never exposed to the client
# and never baked into the bundle. Defaults to http://localhost:8000.
API_URL=http://localhost:8000
